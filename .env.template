# =============================================================================
# Personal Finance App - Environment Variables Template
# =============================================================================
# Copy this file to .env.local and fill in the values for local development.
# Do not commit .env.local to version control.
# =============================================================================

# -----------------------------------------------------------------------------
# Databricks Workspace (Required for local development)
# -----------------------------------------------------------------------------
DATABRICKS_CONFIG_PROFILE=your-profile
DATABRICKS_HOST=https://your-workspace.cloud.databricks.com
DATABRICKS_TOKEN=dapi_your_personal_access_token

# -----------------------------------------------------------------------------
# Databricks SQL (Optional - for real financial data)
# -----------------------------------------------------------------------------
# When all 4 variables are set, the app uses DBSQL for financial data.
# Otherwise, it falls back to sample/dummy data.

DBSQL_SCHEMA=your_catalog.your_schema
DATABRICKS_SERVER_HOSTNAME=your-workspace.cloud.databricks.com
DATABRICKS_HTTP_PATH=/sql/1.0/warehouses/your_warehouse_id
# Note: DATABRICKS_TOKEN is shared with workspace auth above

# -----------------------------------------------------------------------------
# MLflow Tracing (Required for observability)
# -----------------------------------------------------------------------------
# Traces are sent to Databricks MLflow. Use ONE of these:
# MLFLOW_EXPERIMENT_NAME = path (creates experiment if doesn't exist)
# MLFLOW_EXPERIMENT_ID = numeric ID (experiment must already exist)
MLFLOW_EXPERIMENT_NAME=/Users/your-email@company.com/personal-finance-traces
# MLFLOW_EXPERIMENT_ID=123456789

# -----------------------------------------------------------------------------
# Lakebase PostgreSQL (Optional - for chat/profile persistence)
# -----------------------------------------------------------------------------
# Choose ONE of these three connection methods:

# Option 1: Direct connection URL
# LAKEBASE_PG_URL=postgresql://user:password@host:5432/database?sslmode=require

# Option 2: Databricks Apps (auto-set when database resource is added)
# PGHOST=your-lakebase-host
# PGPORT=5432
# PGDATABASE=databricks_postgres
# PGUSER=your-username
# PGSSLMODE=require
# LAKEBASE_INSTANCE_NAME=your-lakebase-instance

# Option 3: Local development with Lakebase instance
# LAKEBASE_INSTANCE_NAME=your-lakebase-instance
# LAKEBASE_DATABASE_NAME=databricks_postgres
# PGUSER=your-username  # Optional, defaults to empty

# Optional: Project ID for Lakebase links in UI
# LAKEBASE_PROJECT_ID=your-project-id

# -----------------------------------------------------------------------------
# External APIs (Optional)
# -----------------------------------------------------------------------------
# News API for financial news feed (https://newsapi.org)
NEWSAPI_KEY=your_newsapi_key

# -----------------------------------------------------------------------------
# Databricks MCP (Optional - for external tool integration)
# -----------------------------------------------------------------------------
# Comma-separated list of MCP connection names configured in Databricks.
# Enables web search, market data, and other external tools.
# Example: you-com-search,serpapi
MCP_CONNECTIONS=you-com-search

# -----------------------------------------------------------------------------
# Databricks Apps Deployment (Required for ./scripts/deploy.sh)
# -----------------------------------------------------------------------------
DATABRICKS_APP_NAME=your-app-name
WORKSPACE_SOURCE_PATH=/Workspace/Users/your-email@company.com/your-app-name

# -----------------------------------------------------------------------------
# App Configuration (Optional)
# -----------------------------------------------------------------------------
ENV=development  # Auto-detected based on .env.local presence
